server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # All routes fall back to index.html so React Router handles navigation.
    # Without this, refreshing /listings would return a 404 from nginx.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
